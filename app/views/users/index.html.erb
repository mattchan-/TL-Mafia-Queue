<% provide(:title, 'All users') %>
<h1>All users</h1>

<% if @users.total_pages > 1 %>
  <hr>
  <%= will_paginate %>
<% end %>

<ul class="users">
  <%= render @users %>
</ul>

<% if @users.total_pages > 1 %>
  <%= will_paginate %>
<% end %>