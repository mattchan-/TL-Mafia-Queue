<% provide(:title, 'All users') %>
<h1>All users</h1>
<hr>

<% if @games.total_pages > 1 %>
  <%= will_paginate %>
<% end %>

<ul class="users">
  <%= render @users %>
</ul>

<% if @games.total_pages > 1 %>
  <%= will_paginate %>
<% end %>