<%= render 'games/index_menu' %>

<div class="game_index_container">
  <table>
    <%= render 'games/index_header' %>

    <% sortedGameList = @games.sort_by { |option| [option.status_id, -option.signups, -option.updated_at.to_i] } %>
    <% sortedGameList.each do |game| %>
      <%= render 'games/listing', game: game %>
    <% end %>
  </table>
</div>