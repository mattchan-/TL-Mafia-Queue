<div class="gamesContainer">
  <table>
    <%= render 'games/index_header' %>

    <% sortedGameList = @games.sort_by { |option| [-option.signups, option.updated_at.to_i] } %>
    <% sortedGameList.each do |game| %>
      <%= render 'games/listing', game: game %>
    <% end %>
  </table>
</div>