<h1>TL Mafia</h1>
<hr>
<table>
  <tr>
    <% if signed_in? && current_user.host_privileges? %>
      <td><%= link_to 'Create New Game', new_game_path, class: "btn btn-primary" %></td>
    <% end %>
    <td><h3>Sort By:</h3></td>
    <td class="sortable" id="status"><%= sortable 'status', 'Status' %></td>
    <td class="sortable" id="updated_at"><%= sortable 'updated_at', 'Recently Updated' %></td>
  </tr>
</table>
<hr>