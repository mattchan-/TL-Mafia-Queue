<b>Unapproved players:</b><br/>
<i class="notes">Click player name to add to game.</i><br/>
<% unapproved_players = @game.players - @game.approved_players %>
<% unapproved_players.each do |user| %>
  <%= link_to user.name, approve_vote_path(user.votes.find_by_game_id(@game)), { method: :post, remote: true } %><br/>
<% end %>